doctype html
html.no-js(lang='')
	head
		meta(charset='utf-8')
		meta(http-equiv='X-UA-Compatible', content='IE=edge')
		meta(name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=0")
		title
		meta(name='description', content='')
		meta(name='viewport', content='width=device-width, initial-scale=1')

		link(rel='stylesheet', href='css/font.css')
		link(rel='stylesheet', href='css/main.css')
		script(src='http://cdnjs.cloudflare.com/ajax/libs/headjs/1.0.3/head.min.js')
		style.
			[data-aload] { background-image: none !important; }
		script.
			// Register custom html tags
			(function(){if(!/*@cc_on!@*/0)return;var e = "component, subcomponent".split(','),i=e.length;while(i--){document.createElement(e[i])}})()
			
			// Async Image Loading
			function aload(t){"use strict";t=t||window.document.querySelectorAll("[data-aload]"),void 0===t.length&&(t=[t]);var a,e=0,r=t.length;for(e;r>e;e+=1)a=t[e],a["LINK"!==a.tagName?"src":"href"]=a.getAttribute("data-aload"),a.removeAttribute("data-aload");return t}
		block head

	body
		div.cognizant-container

			div.hidden
				include ./includes/symbols.svg

			include ../modules/edit-profile-overlay
			include ../modules/smart-search-overlay

			.cogv1-page
				block content
			
			block footer
			footer
				.footer--top
					.container
						.row
							.footer__social-list
								.container
									.row
										each val, index in [{name: 'linkedin'},{name: 'twitter'},{name: 'facebook'},{name: 'rss'}]
											a(href="")
												svg(viewbox='0 0 100 100')
													use(xlink:href='#icon_#{val.name}')
							.footer__subnav
								.container
									.row
										a(href="#", target="_blank") Legal
										a(href="#", target="_blank") Privacy Policy
										a(href="#", target="_blank") Cookie Policy
										a(href="#", target="_blank") Safe Harbor
										span ©2015 Cognizant, all rights reserved

			browsersync
			block scripts
			script.
				head.load(
					{ vendor: './js/vendor.js' }, 
					{ app: './js/app.js' }, 
					function() {
					// Async Image Loading
					aload();
				});
