.dropdown-module
  border-radius 0.1rem
  box-shadow: 0px 1px 4px 0px rgba(gray,.5)
  z-index: 1
  background: white
  height 0
  overflow hidden
  transition height 0.5s ease
  &.is-active
    overflow scroll
  ul
    li
      color darkgray
      fs(1.8rem)
      font-family proxReg
      display block
      line-height 5.6rem
      padding 0 1.3rem
      text-align left
      transition 0.1s ease
      background-color white
      position relative
      span 
          color blue
          font-family proxSbold
      &:after
          content ''
          height 1px
          display block
          background-color gray
      &:last-of-type
        &:after
          background-color transparent
      &:hover
          background-color backgray
          